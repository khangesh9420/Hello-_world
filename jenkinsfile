pipeline {
    agent any  // This defines that the pipeline can run on any available agent

    stages {
        stage('Checkout') {
            steps {
                // Checkout the repository containing your C++ source code
                git 'https://your-repository-url.git'  // Replace with your repository URL
            }
        }
 }

